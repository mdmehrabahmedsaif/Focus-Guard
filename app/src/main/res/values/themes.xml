<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FocusGuard" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Futuristic brand colors -->
        <item name="colorPrimary">@color/bg_midnight</item>
        <item name="colorPrimaryDark">@color/bg_midnight</item>
        <item name="colorAccent">@color/neon_blue</item>
        
        <!-- Background & Status Bar -->
        <item name="android:statusBarColor">@color/bg_midnight</item>
        <item name="android:windowBackground">@color/bg_midnight</item>
        
        <!-- Material Components Style -->
        <item name="materialButtonStyle">@style/Widget.MaterialComponents.Button</item>
    </style>
</resources>
